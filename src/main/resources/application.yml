server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: easyboke

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/easyboke?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 12345678

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  web:
    resources:
      static-locations: classpath:/static/,file:uploads/

  mail:
    host: smtp.qq.com
    port: 587
    username: 3348098119@qq.com
    password: vssdireohkzichid
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

upload:
  path: uploads/
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.easyboke.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.easyboke: debug
    org.springframework.web: info
